export const metadata = {
  title: 'Admin – Ganesh Electricals',
  robots: 'noindex, nofollow',
};

import AdminClient from '@/components/AdminClient';

export default function AdminPage() {
  return <AdminClient />;
}
